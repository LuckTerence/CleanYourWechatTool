# Security Policy

## 安全边界声明（写给使用者）

CleanYourWechatTool 直接操作微信的本地文件。请先理解以下安全设计，再决定是否使用：

- **只读分析**：`scan` 与 `dedup --dry-run` 不修改任何文件。
- **默认废纸篓**：所有清理通过 macOS Finder 移入系统废纸篓，可随时"放回原处"，绝不 `rm` 硬删。
- **数据库绝对隔离**：`db_storage/`（微信 4.0 的 SQLite/WCDB 库与文字消息）在扫描、清理、归档、去重全流程均被强制排除，代码层一票否决。
- **白名单一票否决**：命中防删规则（文件名关键词 / 精确路径）的文件绝不被移动或归档。
- **纯本地运行**：工具不联网、不上传任何聊天或文件数据；配置、状态与审计日志存于本机 `~/.wechat_slim/`。
- **只读第三方库**：本工具不 Hook、不注入微信进程、不调用微信 UI、不发送任何消息。

## 已知技术边界

微信 4.0 的 `db_storage` 数据库为 WCDB/SQLCipher 加密。未提供解密密钥时，工具**无法**获知"某个文件由哪个联系人或群聊发送"，因此白名单按文件名关键词与精确路径维度生效，而非联系人维度。详见 `docs/wechat-storage-slimming-prd.md` §8。

## 支持版本

| 版本 | 支持状态 |
| :--- | :--- |
| v1.0.x（最新） | ✅ 支持 |
| < v1.0 | ❌ 不支持（未发布） |

## 报告安全漏洞

**请不要为此类问题创建公开 issue。**

请通过以下任一渠道私下报告：

1. 仓库页面的 **"Report a vulnerability"**（GitHub Private vulnerability reporting）；
2. 邮件至维护者：`xihuan1127@gmail.com`，主题请以 `[SECURITY]` 开头，并包含：
   - 影响的版本号 / commit
   - 复现步骤（尽量最小化）
   - 影响面与你认为的严重程度

**响应承诺**：收到报告后 72 小时内确认；确认有效后优先修复并发布补丁；在修复可用前不会公开细节。

## 安全修复流程

1. 确认并复现问题；
2. 在私有分支修复并补充回归测试；
3. 通知报告者，协同发布补丁与安全公告；
4. 感谢并致谢（经报告者同意）。
