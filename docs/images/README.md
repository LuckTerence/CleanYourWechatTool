# 截图目录

README 首屏引用的两张截图放在这里：

| 文件名 | 内容 |
|---|---|
| `screenshot-main.png` | 主界面（存储透视 + 三张智能建议卡片 + 一键瘦身按钮） |
| `screenshot-preview.png` | 清理前的核对清单（逐条勾选/排除） |

截图建议：
- macOS：`Cmd+Shift+4` 然后按 `空格`，点击窗口即可只截该窗口（不包含桌面其他内容）；
- 宽度 1200–1600px 即可，无需压缩。

放好文件后无需改动 README，路径已配好。
